# protocol
#http = http://
#https = https://
protocol = https://

# url
base.url = www.example.com

# server's update folder
update.path = /updates/

# server's version file end path
version.path = /package.json

# server's update file end path
package.path = /src.zip

# connect to server's timeout value(default:10000)
connect.timeout = 10000

# read server's data timeout value(default:25000)
read.timeout = 25000